how to install:
1. make sure you have an image titled `icon.png` and a font titled `font.ttf` in the repository folder (font.ttf is not provided by default!)
2. `make` or `make static` for a static executable
